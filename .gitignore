target/
.idea/
.DS_Store

# app builds are large binaries, not source — keep them out of git
apps/

# local secrets — real credentials live only on each dev's machine / CI secrets
src/resources/verona.local.properties
