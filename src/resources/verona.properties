browser_caps.device=DeviceId_125403149G142674
browser_caps.os_version=14.0
video.log=false
network.log=false
tnt.use.local.grid=true
tnt.local.grid.ip=https://bestq.best-quality.in/api/v1/appium/wd/hub

# Local (emulator) runs: APK looked for at apps/<app.file.name>; if missing
# there and app.download.url is set, it's downloaded once and cached.
app.file.name=verona-staging.apk
app.download.url=

# Remote-grid runs: a build URL BestQ's own server can fetch (it can't reach
# this machine's filesystem). The repo is public, so a GitHub Release asset's
# download URL works directly with no auth. Leave blank instead if the app is
# already installed on the target device via the BestQ dashboard.
app.remote.url=https://github.com/rahul-at-verona/test-automation/releases/download/app-build/verona-staging.apk
